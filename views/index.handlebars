<h1>Burgers!</h1>

<div>
    <ul>
        {{#each burgers}}
        <li>
            {{this.burger_name}}
        </li>
        {{/each}}
    </ul>


    <ul>
        {{!--//looping through all of the burgers--}}
        {{#each burgers}} {{#unless this.devoured}} <li> {{this.id}} {{this.burger_name}} <button data-id={{this.id}} class="burger-btn" type = "submit">Devour Me!</button> </li>{{/unless}} {{/each}}
    </ul>

    <ul>
        {{#each burgers}} {{#if this.devoured}} <li>{{this.id}} {{this.burger_name}} <li> {{/if}} {{/each}}

    </ul>
</div>

{{!--
<h1>Cats!</h1>

<h2>Cats that are not sleepy!</h2>

<ul>
    {{#each cats}} {{#unless sleepy}} {{> cats/cat-block sleep=true}} {{/unless}} {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
    {{#each cats}} {{#if sleepy}} {{> cats/cat-block sleep=false}} {{/if}} {{/each}}
</ul>

<h2>Add a Cat</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Cat Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="slee">Sleepy?</label>
        <br>
        <input type="radio" name="sleepy" value="1" checked> Sleepy!
        <br>
        <input type="radio" name="sleepy" value="0"> Awake
    </div>

    <button type="submit">Add Cat</button>
</form> --}}